{
  "title": "Opility B2B Lead Generator Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchTerms": {
      "title": "Search Terms / Categories",
      "type": "array",
      "description": "List of business categories or search keywords (e.g. ['Home Care Agencies', 'Dental Clinics', 'Software Companies'])",
      "editor": "json",
      "default": ["Home Care Agencies", "Healthcare Clinics"]
    },
    "location": {
      "title": "Target Location",
      "type": "string",
      "description": "City, state, or country to target (e.g. 'New York, NY' or 'London, UK')",
      "editor": "textfield",
      "default": "New York, NY"
    },
    "maxResults": {
      "title": "Maximum Leads to Extract",
      "type": "integer",
      "description": "Maximum total number of B2B business leads to scrape",
      "minimum": 1,
      "maximum": 5000,
      "default": 100
    },
    "extractEmails": {
      "title": "Deep Extract Emails & Socials",
      "type": "boolean",
      "description": "Crawl target business websites to extract contact emails and social media profiles (LinkedIn, Facebook, Twitter, Instagram).",
      "default": true
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Use Apify Proxies to bypass rate limits and geographic blocks.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      }
    }
  },
  "required": ["searchTerms", "location"]
}
